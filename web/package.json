{
  "name": "shopify-app-template-node",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start:render": "bash scripts/render-start.sh",
    "render:build": "bash -lc \"set -e;   if [ ! -s package-lock.json ]; then     echo \\\"[build] no or empty lockfile \u2014 creating with npm install --package-lock-only\\\";     npm install --package-lock-only --include=dev;   fi;   if ! npm ci --include=dev; then     echo \\\"[build] npm ci failed \u2014 falling back to npm install\\\";     npm install --include=dev;   fi;   npx prisma generate --schema=prisma/schema.prisma \" ",
    "dev": "node src/index.js",
    "start": "node src/index.js"
  },
  "dependencies": {
    "@prisma/client": "6.16.0",
    "cookie-parser": "^1.4.7",
    "cors": "2.8.5",
    "cross-env": "^7.0.3",
    "dotenv": "^16.6.1",
    "express": "4.21.2",
    "express-rate-limit": "^7.4.0",
    "helmet": "8.1.0",
    "morgan": "1.10.1",
    "node-fetch": "^2.7.0",
    "nodemailer": "^7.0.6",
    "pino": "^9.2.0",
    "resend": "^6.0.2",
    "stripe": "18.5.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "nodemon": "^3.1.0",
    "prisma": "6.16.0"
  }
}
