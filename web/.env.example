# Production-ish (Render)
DATABASE_URL=postgresql://<user>:<pass>@<render-host>:5432/<db>?sslmode=require

# Local shadow DB (safe target for prisma migrate dev shadow)
SHADOW_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/prisma_shadow?schema=shadow
