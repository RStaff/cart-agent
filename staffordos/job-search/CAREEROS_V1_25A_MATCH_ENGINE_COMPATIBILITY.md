# Ashby Match Engine Compatibility

No Ashby records were run through matching diagnostics because the access gate stopped the mission before retrieval and implementation.

The intended compatibility path is existing provider-neutral processing:

`NormalizedJobSourceRecord -> requirement extraction -> source provenance -> evidence linkage -> qualification/preference diagnostics -> frozen V2D offline diagnostics`.

No scoring, ranking, shortlist, CareerFact, CareerEvidence, J002, J003, or J010 behavior changed.
