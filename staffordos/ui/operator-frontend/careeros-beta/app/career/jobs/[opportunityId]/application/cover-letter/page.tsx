import { redirect } from "next/navigation";
import { currentCareerContext } from "../../../../../../lib/career/careerP0Auth";
import { createCoverLetterDraft, getCoverLetterDraft } from "../../../../../../lib/career/careerP0Product.mjs";
import MaterialEditor from "../MaterialEditor";
import GenerateMaterialButton from "../resume/GenerateResumeButton";
export const runtime = "nodejs";
export default async function CoverLetterPage({ params }: { params: Promise<{ opportunityId: string }> }) { const context = await currentCareerContext(); if (!context) redirect("/career/login"); const opportunityId = (await params).opportunityId; const data: any = await getCoverLetterDraft(context, opportunityId); if (data.opportunity.decisionState !== "PURSUE") return <main className="careerShell"><h1>Cover letter</h1><section className="careerWarning"><p>Mark this opportunity Pursue before preparing application materials.</p><a href={`/career/jobs/${opportunityId}`}>Back to job</a></section></main>; if (data.packet.status !== "CURRENT") return <main className="careerShell"><h1>Cover letter</h1><section className="careerWarning"><p>{data.packet.message || "Re-analyze this job before preparing a cover letter."}</p><a href={`/career/jobs/${opportunityId}`}>Re-analyze this job</a></section></main>; const savePath = `/api/career/opportunities/${opportunityId}/application/cover-letter`; return <main className="careerShell"><header className="careerHeader"><div><p className="careerEyebrow">Application preparation</p><h1>Cover letter for {data.opportunity.title}</h1><p className="careerMuted">CareerOS drafts from information you confirmed. Review before using.</p></div><a href={`/career/jobs/${opportunityId}/application/evidence`}>Application evidence</a></header><section className="careerProfilePanel"><h2>Neutral, evidence-based draft</h2><p className="careerMuted">CareerOS does not invent motivation, company admiration, achievements, or qualifications.</p>{data.draft ? <MaterialEditor savePath={savePath} draft={data.draft} /> : <GenerateMaterialButton opportunityId={opportunityId} endpoint={savePath} label="Create cover letter draft" />}</section></main>; }
