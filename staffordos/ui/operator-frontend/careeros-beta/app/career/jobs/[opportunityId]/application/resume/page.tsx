import { redirect } from "next/navigation";
import { currentCareerContext } from "../../../../../../lib/career/careerP0Auth";
import { getResumeDraft } from "../../../../../../lib/career/careerP0Product.mjs";
import ResumeEditor from "./ResumeEditor";
import GenerateResumeButton from "./GenerateResumeButton";
export const runtime = "nodejs";
export default async function ResumePage({ params }: { params: Promise<{ opportunityId: string }> }) { const context = await currentCareerContext(); if (!context) redirect("/career/login"); const opportunityId = (await params).opportunityId; const data: any = await getResumeDraft(context, opportunityId); if (data.opportunity.decisionState !== "PURSUE") return <main className="careerShell"><h1>Resume draft</h1><section className="careerWarning"><p>Mark this opportunity Pursue before preparing application materials.</p><a href={`/career/jobs/${opportunityId}`}>Back to job</a></section></main>; if (data.packet.status !== "CURRENT") return <main className="careerShell"><h1>Resume draft</h1><section className="careerWarning"><p>{data.packet.message || "Re-analyze this job before preparing a resume draft."}</p><a href={`/career/jobs/${opportunityId}`}>Re-analyze this job</a></section></main>; return <main className="careerShell"><header className="careerHeader"><div><p className="careerEyebrow">Application preparation</p><h1>Resume draft for {data.opportunity.title}</h1><p className="careerMuted">CareerOS drafts from information you confirmed. Review and edit this draft before using it.</p></div><a href={`/career/jobs/${opportunityId}/application-evidence`}>Application evidence</a></header><section className="careerProfilePanel"><h2>Target role</h2><p>{data.opportunity.title}{data.opportunity.company ? ` · ${data.opportunity.company}` : ""}</p><p className="careerMuted">This is a job-specific presentation draft. Editing it does not change your career facts or matching authority.</p></section><section className="careerProfilePanel">{data.draft ? <ResumeEditor opportunityId={opportunityId} draft={data.draft} /> : <><h2>Create a truthful draft</h2><p className="careerMuted">CareerOS will use only confirmed experience from the current evidence packet. Missing structure and unsupported requirements will remain flagged for your review.</p><GenerateResumeButton opportunityId={opportunityId} /></>}</section></main>; }
